% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.cumulcalib}
\alias{plot.cumulcalib}
\title{Generates cumulative calibration plots}
\usage{
\method{plot}{cumulcalib}(
  x,
  method = NULL,
  stats_config = list(),
  x2axis = TRUE,
  y2axis = TRUE,
  draw_polygon = TRUE,
  ...
)
}
\arguments{
\item{x}{An object of class cumulcalib generated by cumulcalib()}

\item{method}{Which method to use. See details. If unspecified, uses the default method used in the cumulcalib() call.}

\item{stats_config}{A list specifying the visualization parameters for the
statistical test(s). Defaults to an empty list (\code{list()}), which by
default draws only the statistic line(s) (\code{"v"}, in blue and red),
not their significance threshold(s) (\code{"h"}, at the 0.95 level).
If set to \code{NULL}, no statistical lines are drawn.
Recognized elements are \code{sig} (significance level(s)),
\code{lines} (which lines to draw: include \code{"v"} for the vertical
statistic line and/or \code{"h"} for the horizontal significance
threshold, e.g. \code{"vh"} to draw both), and \code{col} (color(s)).}

\item{x2axis}{If true, draws a second x-axis (on top) showing predicted risks}

\item{y2axis}{If true, draws a second y-axis (on right) showing scaled partial sums}

\item{draw_polygon}{If true, draws the black triangle marker at the origin. Default is TRUE}

\item{...}{Parameters to be passed to plot()}
}
\value{
None
}
\description{
Plot method for objects returned by \code{\link[=cumulcalib]{cumulcalib()}} and \code{\link[=cumulcalibITE]{cumulcalibITE()}}.
}
\details{
For method, options are:
BM1p (or simply BM): One-part Brownian motion
BM2p: Two-part Brownian motion
BB1p: One-part Brownian bridge
BB2p (or simply BB): Two-part Brownian bridge
}
